//
concept of authentication(session) with passport package 
connectivity with database(using mongoDB) to store feedback
concept of encryption to store password 
use of HTML,CSS,JAVASCRIPT
//
->install mongodb
->install node
->type node app.js(to start local server)
->use VS (preferred) and Live server extension (to start local hosting)